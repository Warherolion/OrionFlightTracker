#ifndef HAL_CONF_EXTRA_H
#define HAL_CONF_EXTRA_H

/* Auto-included by system/STM32F4xx/stm32f4xx_hal_conf.h (__has_include).
 * The STM32duino core leaves the SD HAL module disabled by default; STM32SD
 * (SDIO/SDMMC FatFs stack) needs it, so enable it here. */
#define HAL_SD_MODULE_ENABLED

#endif // HAL_CONF_EXTRA_H
